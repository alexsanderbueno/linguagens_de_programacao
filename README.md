# linguagens_de_programacao
Repositório para as aulas de linguagem de programação sobre Java


/* for(double not:notas)
       if (menor<not){
       menor=not;}
       if (maior>not){
       maior>not;}
       System.out.printl("A menor nota é: " + menor);
       System.out.printl("A maior nota é: " + maior); */